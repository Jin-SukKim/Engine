#pragma once
#include "CppRenderer.h"

namespace JE {

class CppRenderer2D : public CppRenderer
{
public:
	//void DrawMeshWireframe(const class Mesh& mesh);

	//void DrawMesh(const Mesh& mesh, const Matrix3x3& mat, const Texture& texture);
	//void DrawMesh(const Mesh& mesh, const Matrix3x3& mat, const Color& color, const Texture& texture);

	//void DrawTriangle(std::array<Vector2D, 3>& triangle, const Mesh& mesh, const Texture& texture);
};
}

